<UserControl x:Class="CapLed.Desktop.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CapLed.Desktop.Views.Login"
             xmlns:users="clr-namespace:CapLed.Desktop.Views.Users"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="#F0F2F5">
        <Border Background="White" 
                CornerRadius="8" 
                Width="400" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Padding="40"
                BorderBrush="#E0E3E7"
                BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            
            <StackPanel>
                <TextBlock Text="CapLed" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#1A2436" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,5"/>
                
                <TextBlock Text="Système de Gestion de Stock" 
                           FontSize="14" 
                           Foreground="#78909C" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,30"/>

                <!-- Email -->
                <TextBlock Text="Adresse Email" 
                           FontWeight="SemiBold" 
                           Foreground="#455A64" 
                           Margin="0,0,0,8"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Height="40" 
                         VerticalContentAlignment="Center"
                         Padding="10,0"
                         Margin="0,0,0,20"/>

                <!-- Password -->
                <TextBlock Text="Mot de passe" 
                           FontWeight="SemiBold" 
                           Foreground="#455A64" 
                           Margin="0,0,0,8"/>
                <PasswordBox Height="40" 
                             VerticalContentAlignment="Center"
                             Padding="10,0"
                             Margin="0,0,0,10"
                             users:PasswordBoxHelper.Attach="True"
                             users:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="#E53935" 
                           FontSize="12" 
                           TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"
                           Margin="0,0,0,15"/>

                <!-- Login Button -->
                <Button Command="{Binding LoginCommand}" 
                        Content="Se connecter" 
                        Height="45" 
                        Background="#2563EB" 
                        Foreground="White" 
                        FontWeight="Bold" 
                        FontSize="16"
                        Cursor="Hand"
                        Margin="0,10,0,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1D4ED8"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#94A3B8"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Loading Indicator -->
                <ProgressBar Height="4" 
                             IsIndeterminate="True" 
                             Margin="0,15,0,0"
                             Visibility="{Binding IsLoggingIn, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
