<UserControl x:Class="CapLed.Desktop.Views.CRM.LeadsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestion des Leads (Prospects)" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox Width="250" Height="30" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Padding="5,0"/>
            <Button Content="🔍 Rechercher" Command="{Binding SearchCommand}" Width="100" Margin="10,0"/>

            <TextBlock Text="Statut :" VerticalAlignment="Center" Margin="20,0,10,0" FontWeight="SemiBold"/>
            <ComboBox Width="150" Height="30" ItemsSource="{Binding StatusList}" SelectedItem="{Binding SelectedStatus}"/>

            <Button Content="🔄 Rafraîchir" Command="{Binding RefreshCommand}" Margin="20,0,0,0" Padding="10,5" Background="#F3F4F6" BorderThickness="1" BorderBrush="#D1D5DB"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Leads}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" SelectionMode="Single" RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" Margin="10" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="Détails du Devis" FontWeight="Bold" Margin="0,0,0,5"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Société : " FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Client.RaisonSociale, TargetNullValue='(Non renseignée)'}"/>
                                <TextBlock Text=" | Message : " FontWeight="SemiBold" Margin="15,0,0,0"/>
                                <TextBlock Text="{Binding MessageClient, TargetNullValue='(Aucun message)'}" TextWrapping="Wrap" MaxWidth="500"/>
                            </StackPanel>
                            
                            <DataGrid ItemsSource="{Binding Lignes}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Référence" Binding="{Binding ArticleReference}" Width="150"/>
                                    <DataGridTextColumn Header="Désignation" Binding="{Binding ArticleName}" Width="250"/>
                                    <DataGridTextColumn Header="Qté Demandée" Binding="{Binding Quantite}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Num Devis" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Date" Binding="{Binding DateCreation, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="120"/>
                <DataGridTextColumn Header="Client/Contact" Binding="{Binding NomContact}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding EmailContact}" Width="150"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Statut}" Width="120" FontWeight="Bold"/>

                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Prendre en charge" 
                                        Command="{Binding DataContext.PrendreEnChargeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0" Padding="5,2" Background="#3B82F6" Foreground="White" BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Statut}" Value="NOUVEAU">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Accepter le devis" 
                                        Command="{Binding DataContext.AccepterDevisCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0" Padding="5,2" Background="#10B981" Foreground="White" BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Statut}" Value="EN_COURS">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Refuser le devis" 
                                        Command="{Binding DataContext.RefuserDevisCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0" Padding="5,2" Background="#EF4444" Foreground="White" BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Statut}" Value="EN_COURS">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Content="Créer Bon de Commande" 
                                        Command="{Binding DataContext.CreerCommandeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0" Padding="5,2" Background="#4B5563" Foreground="White" BorderThickness="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Statut}" Value="ACCEPTE">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
