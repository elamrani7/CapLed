<UserControl x:Class="CapLed.Desktop.Views.Alerts.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CapLed.Desktop.Views.Alerts"
             xmlns:vm="clr-namespace:CapLed.Desktop.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F1F5F9">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,25">
            <StackPanel>
                <TextBlock Text="Alertes de Stock Bas" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Suivi des équipements nécessitant un réapprovisionnement" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>
            
            <Button DockPanel.Dock="Right" 
                    Content="Actualiser" 
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ActionButton}"
                    Background="#334155" 
                    HorizontalAlignment="Right" 
                    Width="140" Height="40"/>
        </DockPanel>

        <!-- Status Messages -->
        <StackPanel Grid.Row="1">
            <Border Background="#FEE2E2" BorderBrush="#EF4444" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#B91C1C" FontWeight="Medium"/>
            </Border>
            <Border Background="#DCFCE7" BorderBrush="#22C55E" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20"
                    Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibility}}">
                <TextBlock Text="{Binding SuccessMessage}" Foreground="#15803D" FontWeight="Medium"/>
            </Border>
        </StackPanel>

        <!-- Main Content -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="1" Effect="{DynamicResource SubtleShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding Alerts}"
                          SelectedItem="{Binding SelectedAlert}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F1F5F9"
                          Background="White"
                          BorderThickness="0"
                          RowHeight="50"
                          SelectionMode="Single">
                    
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCritical}" Value="True">
                                    <Setter Property="Background" Value="#FFF1F0"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                    <Setter Property="Background" Value="#FFFBE6"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Ellipse Width="12" Height="12" HorizontalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCritical}" Value="True">
                                                        <Setter Property="Fill" Value="#EF4444"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                        <Setter Property="Fill" Value="#F59E0B"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="ÉQUIPEMENT" Binding="{Binding EquipmentName}" Width="*" FontWeight="SemiBold"/>
                        
                        <DataGridTextColumn Header="STOCK ACTUEL" Binding="{Binding CurrentQuantity}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="SEUIL MINIMUM" Binding="{Binding Threshold}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="#64748B"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="STATUT" Binding="{Binding Status}" Width="120"/>
                        <DataGridTextColumn Header="CRÉÉ LE" Binding="{Binding CreatedAt, StringFormat='dd/MM/yyyy'}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Action Bar -->
                <Border Grid.Row="1" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="20">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Marquer comme traité"
                                Command="{Binding AcknowledgeCommand}"
                                Style="{StaticResource ActionButton}"
                                IsEnabled="{Binding SelectedAlert, Converter={StaticResource InverseNullToBool}}"
                                Width="200" Height="40"
                                Background="#10B981" />
                    </StackPanel>
                </Border>

                <!-- Loading Overlay -->
                <Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" Background="#80FFFFFF">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="10"/>
                        <TextBlock Text="Chargement des alertes..." Margin="0,15,0,0" Foreground="#64748B" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
