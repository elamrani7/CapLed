services:
  - type: web
    name: capled-api
    runtime: docker
    plan: free
    region: frankfurt
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      # Ajoutez ici vos autres variables d'environnement (Database, SecretKey, etc.)
      # Elles seront demandées par Render lors de l'application du Blueprint.
